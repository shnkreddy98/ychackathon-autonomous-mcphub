# Voyage AI API Key (optional - for semantic tool search embeddings)
# Generates 1024-dimensional embeddings using voyage-4 model
# If unset, app still starts; tool search uses fallback (no semantic ranking).
# Get your key from: https://dash.voyageai.com/
VOYAGE_API_KEY=

# Firecrawl API Key (required for web scraping)
# Get your key from: https://firecrawl.dev
FIRECRAWL_API_KEY=your_firecrawl_api_key_here

# OpenRouter API Key (required for LLM access)
# Get your key from: https://openrouter.ai/
OPENROUTER_API_KEY=your_openrouter_api_key_here

# MongoDB Connection (optional - only needed if running locally)
# For Docker Compose, this is set automatically
MONGODB_URI=mongodb://admin:admin123@localhost:27017/agent_db?authSource=admin

# MCP Server Configuration (optional)
# Port and host for MCP server (if SSE transport is added in future)
MCP_PORT=8002
MCP_HOST=0.0.0.0
